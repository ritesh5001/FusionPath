# Environment Variables for Vercel Deployment
# Copy this file and fill in the values

# ===========================================
# DATABASE (MongoDB Atlas)
# ===========================================
# Get this from MongoDB Atlas: https://cloud.mongodb.com
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>
MONGODB_URI=

# ===========================================
# AUTHENTICATION (NextAuth.js)
# ===========================================
# Generate a secret: openssl rand -base64 32
# Or use: https://generate-secret.vercel.app/32
AUTH_SECRET=

# Google OAuth Credentials
# Get from: https://console.cloud.google.com/apis/credentials
# IMPORTANT: Add your Vercel URLs to authorized redirect URIs:
#   - https://your-app.vercel.app/api/auth/callback/google
#   - https://your-custom-domain.com/api/auth/callback/google (if using custom domain)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# ===========================================
# OPTIONAL (Vercel sets these automatically)
# ===========================================
# NEXTAUTH_URL is automatically set by Vercel
# Only set this if using a custom domain
# NEXTAUTH_URL=https://your-domain.com
